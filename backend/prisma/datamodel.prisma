type User {
  id: ID! @id
  name: String!
  email: String! @unique
  password: String!
  courses: [Course]!
}

type Course {
  id: ID! @id @unique
  createdAt: DateTime! @createdAt
  title: String!
  description: String!
  price: Int!
  language: String!
  image: String
  # user: User!
}
