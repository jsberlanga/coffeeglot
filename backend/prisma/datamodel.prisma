type User {
  id: ID! @id
  name: String!
  email: String! @unique
  password: String!
  courses: [Course]!
}

type Course {
  id: ID! @id @unique
  title: String!
  description: String!
  language: String!
  image: String
  price: Int!
  user: User!
}
