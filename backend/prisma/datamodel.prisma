type User {
  id: ID! @id
  name: String!
  email: String! @unique
  password: String!
  courses: [Course]
  isNative: String
  image: String
  about: String
  experience: String
  education: String
  certifications: String
}

type Course {
  id: ID! @id @unique
  createdAt: DateTime! @createdAt
  title: String!
  details: String!
  price: Int!
  language: String!
  location: String!
  seats: Int!
  startDate: String!
  endDate: String!
  # user: User!
}
